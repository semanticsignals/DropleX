# Acquisition & visualization (tablet_session_visualizer.py)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# CNN training scripts (Torch)
torch>=2.0
scikit-learn>=1.3
seaborn>=0.12

# Salt concentration (train_nacl_classifier.py) — Torch + sklearn stack above

# Random Forest liquid classifier (train_liquid_classifier_rf.py)
joblib>=1.3

# ONNX export (training) and inference (example_model_inference.py)
onnx>=1.14
onnxruntime>=1.15
